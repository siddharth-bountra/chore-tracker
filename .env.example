# Copy to .env.local and fill in your values.
# Use Supabase (recommended) or GAS. If both are set, Supabase is used.

# Supabase (Settings → API in dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Daily 9pm email: report is built from Supabase when this is set (GAS calls /api/today-report?token=...)
REPORT_API_TOKEN=your-report-token

# Google Sheets / Apps Script (optional if using Supabase)
NEXT_PUBLIC_GAS_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
NEXT_PUBLIC_API_TOKEN=your-secret-token
